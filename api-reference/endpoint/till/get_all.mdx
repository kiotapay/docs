---
title: Till Status API
description: Endpoint to fetch the status of a till transaction using the `requestId` and `companyId`.
---

# Till Status API

This API allows a company to fetch the status of a specific till transaction using the `companyId` and `requestId`.

## Endpoint

GET /api/v1/auth/spending/till/status


### Base URL

`https://api.kiotapay.co`

## Query Parameters

| Parameter   | Type     | Description                          | Required |
|-------------|----------|--------------------------------------|----------|
| `companyId` | `string` | The ID of the company initiating the request. | ✅       |
| `requestId` | `string` | The unique ID of the request for which the status is being checked. | ✅       |

## Request Headers

No headers required for this endpoint.

## Interactive API

<InteractiveApi
  method="GET"
  url="https://api.kiotapay.co/api/v1/auth/spending/till/status"
  queryParameters={[
    {
      name: "companyId",
      type: "string",
      description: "The ID of the company initiating the request.",
      required: true,
      example: "4d6e1ea9-5ab8-496a-978f-7e9b2bce80c4"
    },
    {
      name: "requestId",
      type: "string",
      description: "The unique ID of the request for which the status is being checked.",
      required: true,
      example: "20f6da4c-f1fa-4af0-9e5c-72c4e7ac8f57"
    }
  ]}
/>

## Example Curl Request

```bash
curl --location 'https://api.kiotapay.co/api/v1/auth/spending/till/status?companyId=4d6e1ea9-5ab8-496a-978f-7e9b2bce80c4&requestId=20f6da4c-f1fa-4af0-9e5c-72c4e7ac8f57'
